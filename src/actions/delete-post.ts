"use server";

export async function deletePost() {
  // TODO:
  // - revalidate: "/"
  // - revalidate: "/profile/[username]"
  // - revalidate: "/post/[id]" (if user is currently viewing it)
  // - Time Based revalidate: "/explore" 
}
