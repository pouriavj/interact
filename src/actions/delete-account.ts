"use server";

export async function deleteAccount() {
  // TODO:
  // - revalidate: "/" (logged-out state)
  // - revalidate: "/profile/[username]" (profile disappears)
  // - revalidate: "/activity"
  // - revalidate: "/messages"
}
